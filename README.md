

# ระบบรายงานผลการเลือกตั้งแบบเรียลไทม์ (ECT Real-time Election Report 2026)

**Assignment:** ออกแบบ MongoDB Data Model สำหรับระบบรายงานผลเลือกตั้งระดับประเทศ (Thailand Election 2026) ที่รองรับการส่งผลคะแนนมหาศาล (Write Heavy) และการแสดงผล Real-time Dashboard (Read Heavy) พร้อมระบบป้องกันการทุจริต (Anti-Cheat Security)

##  ภาพรวมโครงการ (Project Overview)

ระบบนี้ถูกออกแบบมาเพื่อรองรับข้อมูลขนาดใหญ่:

* หน่วยเลือกตั้ง: **50,000+ หน่วย**
* เขตเลือกตั้ง: **400 เขต**
* พรรคการเมือง: **57 พรรค**
* ผู้มีสิทธิเลือกตั้ง: **52 ล้านคน**

จุดเด่นสำคัญคือสถาปัตยกรรม **"Secure Audit Trail"** ที่ใช้หลักการ Cryptography (Hash Chaining) เพื่อป้องกันการแก้ไขข้อมูลย้อนหลังและป้องกันบัตรเขย่ง

---

##  ระบบป้องกันการทุจริต (Anti-Cheat Mechanisms)

เราออกแบบ Data Model ให้มีความปลอดภัย 3 ระดับ:

1. **Tamper-Proof (ป้องกันการแก้ไข):** ใช้ **Hash Chaining** เชื่อมโยงข้อมูล Transaction แต่ละรายการ (`prev_hash` -> `data_hash`) หากมีการแอบแก้คะแนนย้อนหลัง โซ่ข้อมูลจะขาดทันที (Blockchain-like)
2. **Anti-Ghost Voting (ป้องกันบัตรเขย่ง):** มี Logic ตรวจสอบความสัมพันธ์ของตัวเลข
* `Ballots` (บัตรที่ใช้) ต้อง ≤ `Turnout` (ผู้มาใช้สิทธิ)
* `Turnout` ต้อง ≤ `Eligible Voters` (ผู้มีสิทธิในหน่วย)
* หากผิดเงื่อนไข ระบบจะบันทึกสถานะเป็น **"REJECTED"**


3. **Anti-Impersonation (ป้องกันสวมรอย):** รองรับฟิลด์ `digital_signature` และ `public_key` เพื่อยืนยันตัวตนเจ้าหน้าที่ประจำหน่วย

---

##  การออกแบบฐานข้อมูล (Database Schema)

### 1. `vote_transactions` (Collection หลัก)

เก็บข้อมูลการส่งคะแนนแบบ Transaction Log (1 หน่วยส่งได้หลายครั้ง)

```javascript
{
  "_id": ObjectId("..."),
  "station_id": "S00001",       // รหัสหน่วยเลือกตั้ง
  "ballot_type": "party_list",  // ประเภทบัตร (party_list / constituency)
  "sequence_no": 2,             // ลำดับการส่ง (1, 2, 3...)
  "timestamp": ISODate("..."),
  
  // เนื้อหาข้อมูล (Data Payload)
  "payload": {
    "voter_turnout": 600,       // จำนวนผู้มาใช้สิทธิ
    "total_ballots": 600,       // จำนวนบัตรที่นับได้
    "stats": { "good": 550, "bad": 30, "no_vote": 20 },
    "results": [
       { "id": 1, "score": 300 }, // id พรรค หรือ id ผู้สมัคร
       { "id": 2, "score": 250 }
    ]
  },

  // ความปลอดภัย (Security Fields)
  "integrity_status": "ACCEPTED", // หรือ "REJECTED" ถ้าตัวเลขไม่สัมพันธ์กัน
  "prev_hash": "a1b2c3...",       // Hash ของ Transaction ก่อนหน้า
  "data_hash": "f9e8d7...",       // Hash ของข้อมูลชุดนี้
  "reporter_signature": "SIG_..." // ลายเซ็นดิจิทัล
}

```

### 2. `polling_stations` (Master Data)

เก็บข้อมูลหน่วยเลือกตั้งและกุญแจความปลอดภัย

* `_id`: String (รหัสหน่วย)
* `total_eligible_voters`: Number (จำนวนผู้มีสิทธิ - Hard Cap)
* `station_public_key`: String (กุญแจตรวจสอบลายเซ็น)

---

## 🛠️ วิธีการติดตั้งและใช้งาน (Installation & Usage)

### 1. สิ่งที่ต้องมี (Prerequisites)

* Python 3.x
* MongoDB (ติดตั้ง Local หรือใช้ Atlas)
* Library: `pymongo`, `faker`

### 2. ติดตั้ง Library


pip install pymongo faker


### 3. รันสคริปต์สร้างข้อมูลจำลอง (Data Generation)

สคริปต์นี้จะสร้างข้อมูลหน่วยเลือกตั้ง 50,000+ หน่วย และ Transaction การโหวต 120,000+ รายการ พร้อมคำนวณ Hash และจำลองการโกง (Fraud Simulation)

python3 generate_election_data.py



### 4. การสร้าง Index (Indexing Strategy)

เพื่อประสิทธิภาพสูงสุดในการตอบคำถาม Q1-Q6 ให้รันคำสั่งนี้ใน MongoDB Shell:

```javascript
// Index หลักสำหรับ Dashboard (Q1, Q3, Q4)
db.vote_transactions.createIndex(
  { ballot_type: 1, integrity_status: 1, station_id: 1, sequence_no: -1 }
);

// Index สำหรับเจาะจงหน่วย (Q2, Q5, Q6)
db.vote_transactions.createIndex(
  { station_id: 1, ballot_type: 1, sequence_no: -1 }
);

```

---

##  ชุดคำสั่งค้นหา (Queries Q1-Q6)

ระบบรองรับการตอบคำถามสำคัญดังนี้:

* **Q1:** คะแนนรวมพรรค (Party List) ระดับประเทศ Top 10
* **Q2:** คะแนนรวมผู้สมัครในเขตเลือกตั้ง (Constituency)
* **Q3:** ความคืบหน้าการรายงานผล (Reporting Progress)
* **Q4:** ผลรวมคะแนนแยกตามจังหวัด
* **Q5:** Audit Trail ประวัติการส่งผลของหน่วยเลือกตั้ง (ดู Hash Chain)
* **Q6:** ดูสถานะการส่งผลล่าสุดของหน่วยเลือกตั้ง

